
@{
    ViewBag.Title = "TurnuvaEkle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model VeritabaniKatmani.Turnuva

<div class="breadcrumbs">
    <div class="col-sm-4">
        <div class="page-header float-left">
            <div class="page-title">
                <h1>Turnuva Ekle</h1>
            </div>
        </div>
    </div>

</div>

<div class="col-lg-6">
    <div class="card">

        <div class="card-body">
            <!-- Credit Card -->
            <div id="pay-invoice">
                <div class="card-body">

                    <hr>

                    @using (Html.BeginForm("Ekle", "Turnuva", FormMethod.Post, new { enctype = "multipart/form-data" }))
                    {

                        <div class="form-group">
                            <label for="Adi" class="control-label mb-1">Turnuva Adı</label>
                            @Html.TextBoxFor(m => m.Adi, new { @id = "cc-name", @class = "form-control" })

                        </div>
                        <div class="form-group has-success">
                            <label for="Kategori" class="control-label mb-1">Kategori</label>
                            @Html.DropDownListFor(m => m.KategoriId, (List<SelectListItem>)ViewBag.dgr, new { @id = "select", @class = "form-control" })

                        </div>
                        <div class="form-group has-success">
                            <label for="Kategori" class="control-label mb-1">Yönetici</label>
                            @Html.DropDownListFor(m => m.YoneticiKullaniciId, (List<SelectListItem>)ViewBag.Yon, new { @id = "select", @class = "form-control" })

                        </div>
                        <div class="form-group">
                            <label for="file" class="control-label mb-1">Logo</label>
                            <input type="file" name="file" id="file-input" class="form-control-file" />

                        </div>
                        <div class="form-group">
                            <hr />
                        </div>

                        <div>
                            @if (@ViewBag.Basari == 1)
                            {
                                <div class="alert  alert-success alert-dismissible fade show" role="alert">
                                    <span class="badge badge-pill badge-success">Başarılı</span> Turnuva Kaydedildi.
                                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                        <span aria-hidden="true">×</span>
                                    </button>
                                </div>
                            }
                            <button type="submit" class="btn btn-success btn-lg">
                                <i class="fa fa-dot-circle-o"></i> Kaydet
                            </button>
                            @Html.ActionLink("Turnuva", "Index", "Turnuva", null, new { @class = "btn btn-primary btn-lg" })
                        </div>
                    }
                </div>
            </div>

        </div>
    </div> <!-- .card -->

</div>